[ ] Faire marcher le PullRequest

[ ] Effectuer le PullRequest du sprint 0

[ ] Effectuer le PullRequest du spring 1

Framework
    [x] Creer une annotation Controller pour indiquer qu'une classe est un controller

    FrontServlet
        attribut:
            [x] List<String> listClass (pour stocker les classes annotes Controller)
            a initialiser dans la methode init
Test
    - Creer une classe qui utilise cette annotation Controller  
    - Creer un bout de code au demarrage de l'application web (Conseil: utiliser Listener) ou au premier appel du FrontServlet
    - Dans le web.xml : mettre une variable (pour le nom du package dans lequel on va chercher les controllers)

Creer une classe Utility
    Methode :
        Verifier qu'un annotation se trouve au niveau (classe, attribut,Methode) pour tel classe dans tel package

PullRequest : Limite Dimanche

sprint speciale